namespace NTComponents;

/// <summary>
///     Controls the visual density of popup menu items rendered by FormV2 menu-backed fields.
/// </summary>
public enum NTMenuItemAppearance {
    /// <summary>
    ///     Uses the component's Material 3 default menu item layout.
    /// </summary>
    Default,

    /// <summary>
    ///     Uses a compact single-line menu item layout capped at 38 CSS pixels tall.
    /// </summary>
    Condensed
}
